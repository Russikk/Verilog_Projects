# Synchronous Two-Digit Octal Counter

## 📌 Опис проекту
Цей репозиторій містить RTL-реалізацію та середовище верифікації для **синхронного дворозрядного вісімкового лічильника** (Two-Digit Octal Counter).

**Ключові особливості:**
* **Мова опису:** SystemVerilog.
* **Захист:** Вбудовані синхронізатори для асинхронних сигналів (`Reset`, `Enable`).
* **Верифікація:** SystemVerilog Layered Testbench із використанням підходу Constrained Random Verification.

## 📂 Структура проекту

* **`doc/`** — Звіт.
* **`rtl/`** — Вихідний код дизайну:
    * `top.sv` — Верхній рівень із синхронізаторами.
    * `octal_counter_top.sv` — Ядро лічильника.
    * `counter_3bit.sv` — Базовий модуль розряду.
    * `rst_sync.sv`, `data_sync.sv` — Модулі захисту від метастабільності.
* **`tb/`** — Тестове оточення (Verification Environment):
    * `top_tb.sv` — Тестбенч верхнього рівня.
    * `environment.sv`, `scoreboard.sv`, `driver.sv`, `generator.sv` — Класи верифікації.
    * `transaction.sv` — Опис транзакцій.
    * `counter_if.sv` — Інтерфейс.
* **`sim/`** — Скрипти та логи симуляції:
    * `runme.sh` — Bash-скрипт для запуску.

## 🚀 Як запустити симуляцію

Симуляція та верифікація виконуються у середовищі **Cadence Xcelium**.

1.  Відкрийте термінал Linux.
2.  Перейдіть у папку симуляції:
    ```bash
    cd <шлях_до_проекту>/sim
    ```
3.  Запустіть скрипт виконання:
    ```bash
    sh runme.sh
    ```
4.  Скрипт автоматично скомпілює проект, запустить симуляцію та виведе результати у консоль.
